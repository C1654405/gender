<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Baby Reveal Countdown</title>
  <link rel="stylesheet" href="style.css" />
</head>
<body>
  <div id="countdown-container" class="countdown">
    <h1>A SPECIAL SURPRISE IS ON THE WAY!</h1>
    <div id="timer">Loading...</div>
    <p>Come back here at 6 PM on September 6<sup>th</sup> (UK Time) to find out.</p>
  </div>

  <div id="reveal-container" class="hidden reveal">
    <h1>It's A<br>Baby<br>Boy!</h1>
    <p>The name will be announced in November along with the godparents</p>
    <canvas id="confetti-canvas"></canvas>
  </div>

  <script src="script.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/canvas-confetti@1.5.1/dist/confetti.browser.min.js"></script>
</body>
</html>

body {
  margin: 0;
  font-family: 'Segoe UI', sans-serif;
  background: #fff8f1;
  color: #6e5849;
  text-align: center;
  overflow-x: hidden;
}

.countdown, .reveal {
  padding: 3rem 1rem;
}

#timer {
  font-size: 4rem;
  font-weight: bold;
  margin: 1rem 0;
  background: #f3e4d4;
  display: inline-block;
  padding: 1rem 2rem;
  border-radius: 1rem;
}

.hidden {
  display: none;
}

.reveal {
  background-color: #e0f0ff;
  color: #0057a8;
  position: relative;
}

.reveal h1 {
  font-size: 3rem;
  line-height: 1.3;
  margin-top: 2rem;
}

#confetti-canvas {
  position: fixed;
  pointer-events: none;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
}

const countdownContainer = document.getElementById("countdown-container");
const revealContainer = document.getElementById("reveal-container");
const timer = document.getElementById("timer");

const revealTime = new Date("2025-09-06T17:00:00Z"); // 6 PM UK = 5 PM UTC

function updateCountdown() {
  const now = new Date();
  const diff = revealTime - now;

  if (diff <= 0) {
    countdownContainer.classList.add("hidden");
    revealContainer.classList.remove("hidden");
    launchConfetti();
    return;
  }

  const hours = Math.floor(diff / 1000 / 60 / 60);
  const minutes = Math.floor((diff / 1000 / 60) % 60);
  const seconds = Math.floor((diff / 1000) % 60);

  timer.textContent = `${String(hours).padStart(2, '0')}:${String(minutes).padStart(2, '0')}:${String(seconds).padStart(2, '0')}`;
}

function launchConfetti() {
  const duration = 10 * 1000;
  const end = Date.now() + duration;

  (function frame() {
    confetti({
      particleCount: 4,
      angle: 60,
      spread: 55,
      origin: { x: 0 },
    });
    confetti({
      particleCount: 4,
      angle: 120,
      spread: 55,
      origin: { x: 1 },
    });

    if (Date.now() < end) {
      requestAnimationFrame(frame);
    }
  })();
}

updateCountdown();
setInterval(updateCountdown, 1000);

